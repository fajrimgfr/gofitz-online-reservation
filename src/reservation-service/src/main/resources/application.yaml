spring:
  application:
    name: reservation-service

  datasource:
    url: jdbc:postgresql://db-product-service:5432/product
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  
  data:
    redis:
      host: localhost
      port: 6379
      password: hai
      database: 0
      timeout: 5000 # ms

server:
  port: 8082
